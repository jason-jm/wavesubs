# 产品定义：视频字幕生成与翻译工具（Mac）

> 产品名：**Wave Subs**（2026-08-23 定名，此前工作代号 SubFlow）。版本：v0.1 草案，2026-07-19。

## 1. 一句话定位

一款 Mac 桌面应用：导入任意常见格式的视频，自动识别原声语言并转写为文字，翻译成目标语言（默认中文），生成可直接被播放器加载的 SRT / ASS 字幕文件。

**核心解决的唯一问题：拿到一个没有字幕的视频 → 得到一份高质量的目标语言字幕文件。**

## 2. 目标用户

- **首要用户（即本项目发起人的场景）**：收藏了大量外语影片（英语、日语及其他语种，MKV/MP4 等容器），希望离线批量生成中文字幕，观影时外挂加载。
- 通用化后覆盖：
  - 看外语课程 / 演讲 / 纪录片，需要母语字幕的学习者；
  - 需要快速给素材配字幕粗稿的视频创作者、译制组；
  - 对隐私敏感、不希望把媒体上传云端的用户（本地识别是一等公民）。

## 3. 核心用户流程（Happy Path）

```
拖入视频文件（可多个）
  → 应用探测音轨，自动检测源语言（可手动指定）
  → 本地语音识别，生成带时间轴的原文字幕
  → 按所选翻译引擎翻译为目标语言（默认中文）
  → 预览 / 轻量编辑
  → 导出 SRT 或 ASS（原文、译文或双语）到视频同目录，文件名与视频一致
```

导出命名默认 `影片名.zh.srt`（与主流播放器的外挂字幕自动匹配规则一致），确保「以后看片直接加载」零配置。

## 4. 功能范围

### P0 — MVP（先把核心问题解决好）

| 模块 | 内容 |
|---|---|
| 导入 | 拖拽 / 文件选择导入视频；支持 MKV、MP4、AVI、MOV、TS、WebM 等 ffmpeg 可解的容器；多音轨时可选择音轨 |
| 识别 | 本地 Whisper（whisper.cpp，Metal 加速）；源语言自动检测 + 手动覆盖；应用内下载/管理模型（small→large-v3） |
| 翻译 | 翻译引擎抽象层；MVP 先接 1 个 LLM 引擎（OpenAI 兼容接口，可填任意兼容服务的 Base URL + Key）；目标语言默认中文、可选 |
| 导出 | SRT、ASS；模式：仅译文 / 仅原文 / 双语（译文在上原文在下）；默认导出到视频同目录、语言后缀命名 |
| 任务 | 任务队列：多个视频排队处理，显示阶段进度（抽音频→识别→翻译），可取消；失败可重试 |
| 编辑 | 字幕条目列表：可改文本、合并/拆分条目、微调时间轴（MVP 不做完整时间轴拖拽编辑器） |

### P1 — 让它好用

- 多翻译引擎：Claude、DeepL、Ollama（本地离线翻译兜底）等，统一配置界面，可按任务选择；
- 云端 ASR 可选引擎（OpenAI Whisper API 等），与本地引擎同接口；
- 视频预览播放 + 字幕实时叠加校对（MKV 通过 ffmpeg 快速 remux 供预览）；
- 翻译质量增强：整片上下文分批翻译、人名/术语一致性、可自定义术语表和提示词；
- 批量文件夹导入、完成后系统通知。

### P2 — 通用化增强

- 已有字幕翻译模式（导入 SRT/ASS 只做翻译，跳过识别）；
- 说话人分离（diarization）标注；
- ASS 样式模板（字体、描边、位置）；
- 导出内嵌软字幕的 MKV（ffmpeg mux，不转码）。

### 非目标（明确不做）

- 不做视频剪辑 / 转码工具；
- 不做硬字幕烧录（至多 P2 之后再议）；
- 不做字幕站点搜索下载；
- MVP 不做 Windows/Linux（但 Electron 技术栈保留可能性）。

## 5. 关键产品决策

1. **本地优先**：识别默认走本地 whisper.cpp，免费、离线、隐私可控；云端引擎只是可选加速/提质项。
2. **翻译引擎可插拔**：定义统一 `TranslationProvider` 接口，任何引擎（LLM API / DeepL / 本地 Ollama）都是一个实现；用户在设置中配置，任务级可选。
3. **目标语言不写死**：默认中文，但只是一个默认值；源语言自动检测。产品对任意「X 语 → Y 语」通用。
4. **字幕文件是唯一交付物**：一切设计围绕「导出一份能被 IINA/Infuse/VLC 直接识别加载的字幕」这个终点。
5. **分发方式**：直接分发 DMG（自签名 + 公证），不上 App Store——避免沙盒对 ffmpeg/模型下载/任意目录写入的限制。

## 6. 技术架构（Electron + TypeScript）

```
┌────────────────────────── Electron ──────────────────────────┐
│  Renderer（React + Vite + TS）                                │
│    导入/队列 UI · 字幕编辑表 · 设置（引擎/模型/Key）           │
│                    ▲ IPC (typed, contextBridge)               │
│  Main Process（TS）                                           │
│    ├─ JobQueue        任务编排：状态机 + 进度事件              │
│    ├─ MediaService    封装 ffmpeg/ffprobe（子进程）            │
│    │     探测容器/音轨 → 抽取 16kHz 单声道 WAV                 │
│    ├─ AsrService      AsrProvider 接口                        │
│    │     ├─ WhisperCppProvider（捆绑 Metal 版二进制，子进程）   │
│    │     └─ CloudAsrProvider（P1）                            │
│    ├─ TranslateService TranslationProvider 接口               │
│    │     ├─ OpenAICompatibleProvider（MVP）                   │
│    │     └─ Claude / DeepL / Ollama …（P1）                   │
│    ├─ SubtitleCore    分段/断句规则 · SRT/ASS 序列化           │
│    └─ ModelManager    Whisper 模型下载、校验、存储             │
└──────────────────────────────────────────────────────────────┘
```

要点：

- **ffmpeg / whisper.cpp 以静态二进制捆绑**，子进程调用，解析 stderr/JSON 上报进度；不用 native addon，避免 Electron ABI 编译地狱。
- **识别输出统一为内部字幕模型** `Cue { id, start, end, text, translation? }`，识别、翻译、编辑、导出全部围绕它。
- **断句与排版规则**在 SubtitleCore 中独立实现（每条最大时长/字符数、CPS 限制、按标点合并），这是字幕「像人做的」的关键，与引擎解耦。
- **翻译按批次带上下文**：把相邻若干条原文一起交给 LLM，要求按编号返回对应译文，兼顾语境与对齐。
- API Key 存 macOS Keychain（`safeStorage`），不落明文。

## 7. 里程碑

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| M0 骨架 | Electron+Vite+React+TS 工程、ffmpeg/whisper.cpp 集成打通 | 命令链路能把一个 MP4 变成原文 SRT（无 UI 也行） |
| M1 MVP | P0 全部功能 | 拖入一部 MKV 日语片 → 得到可被 IINA 加载的中文 SRT/双语 ASS |
| M2 好用 | P1 功能 | 多引擎切换、预览校对、批量处理稳定 |
| M3 通用 | P2 功能 + 打磨分发 | DMG 公证分发，非开发者可安装使用 |

## 8. 风险与对策

- **长片识别耗时/内存**：按 VAD 或固定窗口分段送入 whisper；队列串行执行识别任务。
- **翻译成本**：按 token 计费的引擎在任务开始前估算字数并提示；提供 Ollama 本地免费兜底。
- **Chromium 不能直接播 MKV**：预览用 ffmpeg 无损 remux 成 MP4 临时文件（P1 再做，MVP 不含播放器）。
- **模型体积（large-v3 约 3GB）**：默认推荐 small/medium，设置中可换；下载断点续传。
